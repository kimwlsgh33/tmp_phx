{
  "scripts": {
    "deploy": "cd .. && mix assets.deploy && rm -f _build/esbuild*"
  },
  "dependencies": {
    "@ffmpeg/ffmpeg": "^0.12.15",
    "cropperjs": "^1.6.2",
    "localforage": "^1.10.0"
  }
}
